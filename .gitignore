# .NET build outputs (auto-generated when you run dotnet build/run)
bin/
obj/

# Development settings with connection strings (has secrets, don't push)
**/appsettings.Development.json

# Kubernetes secrets (has base64 encoded connection strings, don't push)
**/*secret.yaml

# Azure config script (has connection strings if created, don't push)
azure-config.sh

# Mac OS system file (appears automatically on Mac)
.DS_Store
